/* DBMS & SQL for Placements Ecosystem Map — graph data.
   Loaded by maps/dbms-placements.html and (lazily) by the homepage search.
   Edit nodes/links here; the rendering shell lives in assets/map.js. */
(function () {
  'use strict';
  const yt = (q) => 'https://www.youtube.com/results?search_query=' + encodeURIComponent(q);
  const pg = (p) => 'https://www.postgresql.org/docs/current/' + p;

  const CATS = {
    foundations:{name:'Foundations', color:'#a78bfa'},
    er:{name:'ER Modelling', color:'#60a5fa'},
    relational:{name:'Relational Model & Keys', color:'#34d399'},
    normalization:{name:'FDs & Normalization', color:'#f472b6'},
    sql:{name:'SQL Core', color:'#fbbf24'},
    sqladv:{name:'Advanced SQL', color:'#fb923c'},
    txn:{name:'Transactions & Isolation', color:'#f87171'},
    cc:{name:'Concurrency Control', color:'#e879f9'},
    recovery:{name:'Recovery', color:'#c4b5fd'},
    indexing:{name:'Storage, Indexing & Queries', color:'#2dd4bf'},
    dist:{name:'NoSQL & Scaling', color:'#818cf8'},
    engines:{name:'Database Engines', color:'#94a3b8'},
    resources:{name:'Books, Courses & Practice', color:'#facc15'}
  };

  const NODES = [
    // ---------- FOUNDATIONS ----------
    {id:'dbms', label:'Database Management System', cat:'foundations', imp:4,
     desc:`Software that stores, retrieves and protects shared data, so applications don't manage files directly. It provides a data model, a query language, concurrent access, crash recovery and access control. Placement interviews probe three areas above all: schema design (keys, normalization), SQL, and transactions (ACID, isolation, locking).`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Database'},{t:'Video',u:yt('DBMS full course for placements')}]},
    {id:'file-vs-dbms', label:'File System vs DBMS', cat:'foundations', imp:2,
     desc:`The classic opening question. Storing data in plain files leads to redundancy and inconsistency, awkward ad-hoc queries, no safe concurrent access, no atomicity when a crash hits mid-update, and weak security. A DBMS solves each of these, at the cost of complexity and overhead.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Database#History'}]},
    {id:'three-schema', label:'Three-Schema Architecture', cat:'foundations', imp:3,
     desc:`ANSI/SPARC splits a database into external views (what each user sees), the conceptual schema (the logical tables and constraints) and the internal schema (physical storage). The mappings between levels give data independence: physical independence lets storage change without touching the logical schema; logical independence lets the schema change without breaking views.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Three-schema_approach'},{t:'Guide',u:'https://en.wikipedia.org/wiki/Data_independence'}]},
    {id:'data-models', label:'Data Models', cat:'foundations', imp:2,
     desc:`How data is structured and related: hierarchical and network models came first, the relational model won for general-purpose use, and document, key-value, wide-column and graph models returned with NoSQL. The ER model sits a level above as a conceptual design tool.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Database_model'}]},

    // ---------- ER MODELLING ----------
    {id:'er-model', label:'Entity-Relationship Model', cat:'er', imp:4,
     desc:`Chen's conceptual design notation: entities (things), attributes (their properties) and relationships between them, drawn as an ER diagram before any table exists. Interview design questions ("model a library / hospital / e-commerce site") expect an ER diagram first, then its conversion to tables.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Entity%E2%80%93relationship_model'},{t:'Video',u:yt('ER diagram DBMS explained')}]},
    {id:'attributes', label:'Entities & Attribute Types', cat:'er', imp:3,
     desc:`An entity set is a collection of similar entities, identified by a key attribute. Attributes can be simple or composite (name to first/last), single- or multi-valued (phone numbers, drawn with a double oval), stored or derived (age from date of birth, a dashed oval). Multi-valued attributes become separate tables during mapping.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Entity%E2%80%93relationship_model#Entity%E2%80%93relationship_model'}]},
    {id:'cardinality', label:'Relationships & Cardinality', cat:'er', imp:3,
     desc:`Relationships have a degree (binary, ternary), cardinality ratios (1:1, 1:N, M:N) and participation constraints (total: every entity must participate, drawn as a double line; partial otherwise). Cardinality decides where the foreign key goes when mapping: the N side for 1:N, and a separate junction table for M:N.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Cardinality_(data_modeling)'}]},
    {id:'weak-entity', label:'Weak Entities', cat:'er', imp:2,
     desc:`An entity with no key of its own, identified by its owner's key plus a partial key (discriminator): a dependent of an employee, a room in a building. It always has total participation in its identifying relationship and maps to a table whose primary key includes the owner's key.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Weak_entity'}]},
    {id:'eer', label:'Generalization, Specialization & Aggregation', cat:'er', imp:2,
     desc:`Extended ER features. Specialization splits an entity into subtypes (employee into engineer and manager); generalization merges similar entities into a supertype; both form an IS-A hierarchy with disjoint/overlapping and total/partial constraints. Aggregation treats a relationship as an entity so it can take part in another relationship.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Enhanced_entity%E2%80%93relationship_model'}]},
    {id:'er-to-tables', label:'ER to Relational Mapping', cat:'er', imp:3,
     desc:`Rules for turning a diagram into a schema: each strong entity becomes a table, a weak entity's table borrows its owner's key, 1:N puts a foreign key on the N side, M:N gets its own table keyed by both foreign keys, and multi-valued attributes get their own tables. A common exam question is the minimum number of tables a diagram needs.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Associative_entity'},{t:'Video',u:yt('ER diagram to relational tables minimum number of tables')}]},

    // ---------- RELATIONAL MODEL & KEYS ----------
    {id:'relational-model', label:'Relational Model', cat:'relational', imp:3,
     desc:`Codd's 1970 model: data lives in relations (tables) of tuples (rows) over attributes (columns), each attribute drawn from a domain. Relations are sets, so order doesn't matter and duplicates aren't allowed in theory (SQL tables relax this). The foundation for keys, normalization, relational algebra and SQL itself.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Relational_model'},{t:'Paper',u:'https://www.seas.upenn.edu/~zives/03f/cis550/codd.pdf'}]},
    {id:'keys', label:'Keys (Super, Candidate, Primary)', cat:'relational', imp:4,
     desc:`A super key is any attribute set that uniquely identifies a row; a candidate key is a minimal super key; the primary key is the candidate key you choose (not null, unique), and the rest are alternate keys. Composite keys span several columns. "How many super keys does R have?" is a staple question: each candidate key contributes every superset of itself.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Candidate_key'},{t:'Guide',u:'https://en.wikipedia.org/wiki/Superkey'}]},
    {id:'foreign-key', label:'Foreign Keys & Referential Integrity', cat:'relational', imp:3,
     desc:`A foreign key column must match a primary (or unique) key in the referenced table, or be NULL. Referential actions control what happens when the parent row changes: CASCADE, SET NULL, SET DEFAULT, RESTRICT / NO ACTION. It is how the ER model's relationships survive into the schema.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Foreign_key'},{t:'Docs',u:pg('ddl-constraints.html#DDL-CONSTRAINTS-FK')}]},
    {id:'integrity-constraints', label:'Integrity Constraints', cat:'relational', imp:3,
     desc:`Rules the DBMS enforces on every write: domain constraints (type, CHECK), entity integrity (primary key not NULL), referential integrity (foreign keys), NOT NULL and UNIQUE. Declaring them in the schema is safer than validating in application code, because every path to the data gets checked.`,
     links:[{t:'Docs',u:pg('ddl-constraints.html')},{t:'Guide',u:'https://en.wikipedia.org/wiki/Data_integrity'}]},
    {id:'relational-algebra', label:'Relational Algebra', cat:'relational', imp:3,
     desc:`The procedural query language beneath SQL. Its operators are select (σ, filter rows), project (π, pick columns), union, set difference, Cartesian product and rename, plus derived ones: joins (natural, theta, outer), intersection and division ("students who took all courses"). Query optimizers rewrite algebra expressions into cheaper equivalent forms.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Relational_algebra'},{t:'Video',u:yt('relational algebra DBMS operators division')}]},
    {id:'relational-calculus', label:'Relational Calculus', cat:'relational', imp:1,
     desc:`The declarative counterpart to algebra: tuple relational calculus (TRC) and domain relational calculus (DRC) describe what to retrieve with logical formulas rather than how. Safe calculus expressions are exactly as expressive as relational algebra (Codd's theorem). Mostly an exam topic; SQL borrowed its declarative style.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Tuple_relational_calculus'}]},

    // ---------- FDs & NORMALIZATION ----------
    {id:'fd', label:'Functional Dependencies', cat:'normalization', imp:4,
     desc:`X → Y means any two rows that agree on X must agree on Y. FDs capture the business rules of a schema (roll number determines name) and are the input to everything in this cluster: finding keys, testing normal forms and deciding how to decompose. Trivial FDs (Y ⊆ X) always hold.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Functional_dependency'},{t:'Video',u:yt('functional dependency DBMS')}]},
    {id:'closure', label:"Attribute Closure & Armstrong's Axioms", cat:'normalization', imp:3,
     desc:`Armstrong's axioms (reflexivity, augmentation, transitivity) derive every implied FD. In practice you compute X+, the closure of X: everything X determines. If X+ covers all attributes, X is a super key; minimise it to get candidate keys. This is the core mechanical skill for normalization questions.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Armstrong%27s_axioms'},{t:'Video',u:yt('attribute closure find candidate keys DBMS')}]},
    {id:'minimal-cover', label:'Minimal (Canonical) Cover', cat:'normalization', imp:2,
     desc:`The smallest equivalent FD set: single attributes on the right, no extraneous attributes on the left, no redundant FDs. It's the starting point for the 3NF synthesis algorithm, and a frequent GATE-style exercise.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Canonical_cover'}]},
    {id:'anomalies', label:'Update Anomalies', cat:'normalization', imp:3,
     desc:`Why normalization exists. A poorly designed table storing repeated facts causes insertion anomalies (can't add a department without an employee), update anomalies (change one copy, forget another) and deletion anomalies (removing the last employee loses the department). Each normal form removes a class of the redundancy behind them.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Database_normalization#Objectives'}]},
    {id:'1nf', label:'First Normal Form (1NF)', cat:'normalization', imp:2,
     desc:`Every attribute holds a single atomic value: no repeating groups, arrays or comma-separated lists in a cell. Fix it by moving multi-valued data into rows or a separate table. Most relational tables are in 1NF by construction.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/First_normal_form'}]},
    {id:'2nf', label:'Second Normal Form (2NF)', cat:'normalization', imp:2,
     desc:`1NF, plus no non-prime attribute depends on only part of a candidate key (no partial dependency). Only matters when a key is composite: in (student, course) → grade with student → name, name must move out. A table with only single-attribute candidate keys is automatically 2NF.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Second_normal_form'}]},
    {id:'3nf', label:'Third Normal Form (3NF)', cat:'normalization', imp:3,
     desc:`2NF, plus no transitive dependency of a non-prime attribute on a key. Formally, for every non-trivial X → A, either X is a super key or A is a prime attribute (part of some candidate key). A 3NF decomposition that is lossless and dependency-preserving always exists, which is why it's the usual practical target.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Third_normal_form'}]},
    {id:'bcnf', label:'Boyce-Codd Normal Form (BCNF)', cat:'normalization', imp:3,
     desc:`Stricter than 3NF: for every non-trivial X → A, X must be a super key, with no exception for prime attributes. BCNF removes all FD-based redundancy, but a BCNF decomposition may be unable to preserve every dependency. "3NF vs BCNF" and "find the highest normal form" are very common questions.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Boyce%E2%80%93Codd_normal_form'},{t:'Video',u:yt('BCNF vs 3NF highest normal form question')}]},
    {id:'4nf-5nf', label:'4NF & 5NF', cat:'normalization', imp:1,
     desc:`Beyond FDs. 4NF removes non-trivial multivalued dependencies (a teacher's independent sets of subjects and books stored in one table); 5NF (project-join normal form) removes join dependencies not implied by keys. Rarely asked beyond their definitions.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Fourth_normal_form'},{t:'Guide',u:'https://en.wikipedia.org/wiki/Fifth_normal_form'}]},
    {id:'decomposition', label:'Lossless & Dependency-Preserving Decomposition', cat:'normalization', imp:3,
     desc:`Splitting a table must not lose information. It is lossless if joining the parts gives back exactly the original: for a split into R1 and R2, the common attributes must be a key of one of them. It is dependency-preserving if every original FD can be checked within a single part. Lossless is mandatory; preservation is what BCNF sometimes has to give up.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Lossless_join_decomposition'},{t:'Video',u:yt('lossless join dependency preserving decomposition')}]},
    {id:'denormalization', label:'Denormalization', cat:'normalization', imp:2,
     desc:`Deliberately adding redundancy back (duplicated columns, precomputed aggregates, materialized views) to avoid expensive joins on read-heavy paths, accepting the cost of keeping copies in sync. Common in analytics warehouses and NoSQL schemas. A good interview answer names the trade-off, not just the definition.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Denormalization'}]},

    // ---------- SQL CORE ----------
    {id:'sql', label:'SQL', cat:'sql', imp:4,
     desc:`The declarative language for relational databases: you describe the result and the optimizer decides how to compute it. The ISO standard is implemented with dialect differences by PostgreSQL, MySQL, SQLite, Oracle and SQL Server. Placement OAs often include one or two SQL questions, and interviews ask you to write queries live.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/SQL'},{t:'Docs',u:pg('sql.html')}]},
    {id:'sql-sublanguages', label:'DDL, DML, DCL & TCL', cat:'sql', imp:3,
     desc:`SQL's command groups: DDL defines structure (CREATE, ALTER, DROP, TRUNCATE), DML manipulates rows (SELECT, INSERT, UPDATE, DELETE), DCL controls access (GRANT, REVOKE), and TCL manages transactions (COMMIT, ROLLBACK, SAVEPOINT). A quick-fire classification question.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Data_definition_language'},{t:'Guide',u:'https://en.wikipedia.org/wiki/Data_manipulation_language'}]},
    {id:'delete-truncate-drop', label:'DELETE vs TRUNCATE vs DROP', cat:'sql', imp:2,
     desc:`DELETE removes chosen rows (with WHERE), fires row triggers and is logged per row. TRUNCATE removes all rows quickly by deallocating storage and resets identity counters. DROP removes the table itself. Nuance worth knowing: TRUNCATE commits implicitly in MySQL and Oracle but is transactional in PostgreSQL.`,
     links:[{t:'Docs',u:pg('sql-truncate.html')},{t:'Docs',u:'https://dev.mysql.com/doc/refman/8.4/en/truncate-table.html'}]},
    {id:'select-basics', label:'SELECT, WHERE & ORDER BY', cat:'sql', imp:3,
     desc:`The building blocks: choose columns, filter rows with WHERE (comparison, BETWEEN, IN, LIKE with % and _), remove duplicates with DISTINCT, sort with ORDER BY, and page with LIMIT/OFFSET (FETCH FIRST in standard SQL, TOP in SQL Server).`,
     links:[{t:'Docs',u:pg('tutorial-select.html')},{t:'Tool',u:'https://sqlbolt.com/'}]},
    {id:'group-by', label:'Aggregates, GROUP BY & HAVING', cat:'sql', imp:3,
     desc:`Aggregate functions (COUNT, SUM, AVG, MIN, MAX) collapse rows into one value per group. WHERE filters rows before grouping and HAVING filters groups after. Every non-aggregated column in SELECT must appear in GROUP BY. COUNT(*) counts rows while COUNT(col) skips NULLs, a frequent trap.`,
     links:[{t:'Docs',u:pg('tutorial-agg.html')},{t:'Video',u:yt('SQL group by having interview questions')}]},
    {id:'joins', label:'SQL Joins', cat:'sql', imp:4,
     desc:`INNER JOIN keeps matching pairs; LEFT/RIGHT OUTER keep every row from one side with NULLs for missing matches; FULL OUTER keeps both; CROSS JOIN is the Cartesian product; a self join relates a table to itself (employee to manager). "Find rows with no match" is a LEFT JOIN plus WHERE right.id IS NULL. The most-tested SQL topic.`,
     links:[{t:'Docs',u:pg('tutorial-join.html')},{t:'Guide',u:'https://en.wikipedia.org/wiki/Join_(SQL)'}]},
    {id:'subqueries', label:'Subqueries & Correlated Subqueries', cat:'sql', imp:3,
     desc:`A query nested inside another: scalar subqueries return one value, IN/EXISTS subqueries test membership, derived tables sit in FROM. A correlated subquery references the outer row and conceptually re-runs per row ("employees earning more than their department's average"). Many can be rewritten as joins or window functions.`,
     links:[{t:'Docs',u:pg('functions-subquery.html')},{t:'Guide',u:'https://en.wikipedia.org/wiki/Correlated_subquery'}]},
    {id:'set-ops', label:'UNION, INTERSECT & EXCEPT', cat:'sql', imp:2,
     desc:`Combine results of union-compatible queries (same number and types of columns). UNION removes duplicates while UNION ALL keeps them and is faster; INTERSECT keeps common rows; EXCEPT (MINUS in Oracle) keeps rows only in the first result. MySQL added INTERSECT and EXCEPT only in 8.0.31.`,
     links:[{t:'Docs',u:pg('queries-union.html')}]},
    {id:'null-handling', label:'NULL & Three-Valued Logic', cat:'sql', imp:3,
     desc:`NULL means unknown, so comparisons with it yield UNKNOWN rather than true or false: use IS NULL, never = NULL. WHERE keeps only TRUE rows, aggregates ignore NULLs, and COALESCE supplies defaults. The classic trap: NOT IN against a subquery containing a NULL returns no rows at all, so prefer NOT EXISTS.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Null_(SQL)'},{t:'Docs',u:pg('functions-comparison.html')}]},
    {id:'query-order', label:'Logical Query Processing Order', cat:'sql', imp:2,
     desc:`SQL runs in a different order from how it's written: FROM/JOIN, WHERE, GROUP BY, HAVING, SELECT (including window functions), DISTINCT, ORDER BY, LIMIT. It explains why a SELECT alias can't be used in WHERE but can in ORDER BY, and why window functions can't be filtered without a subquery or CTE.`,
     links:[{t:'Docs',u:pg('sql-select.html')}]},

    // ---------- ADVANCED SQL ----------
    {id:'window-functions', label:'Window Functions', cat:'sqladv', imp:3,
     desc:`Compute over a set of related rows without collapsing them: ROW_NUMBER, RANK, DENSE_RANK, LAG/LEAD, running SUM, all with OVER (PARTITION BY … ORDER BY …). They make "top N per group", running totals, gaps-and-islands and day-over-day change easy, and are asked increasingly often in data-heavy roles.`,
     links:[{t:'Docs',u:pg('tutorial-window.html')},{t:'Docs',u:pg('functions-window.html')},{t:'Video',u:yt('SQL window functions rank dense_rank row_number')}]},
    {id:'nth-salary', label:'Nth Highest Salary Pattern', cat:'sqladv', imp:2,
     desc:`The most famous SQL interview question, with several valid answers: DENSE_RANK in a subquery, LIMIT 1 OFFSET N-1 over DISTINCT salaries, or a correlated COUNT(DISTINCT) subquery. The follow-ups probe ties (RANK vs DENSE_RANK), a missing Nth value (return NULL) and per-department variants.`,
     links:[{t:'Tool',u:'https://leetcode.com/problems/nth-highest-salary/'},{t:'Video',u:yt('nth highest salary SQL all approaches')}]},
    {id:'cte', label:'CTEs & Recursive Queries', cat:'sqladv', imp:2,
     desc:`WITH introduces named temporary result sets that make multi-step queries readable. WITH RECURSIVE walks hierarchies and graphs (org charts, category trees, generating series) by repeatedly joining a working set until it stops growing.`,
     links:[{t:'Docs',u:pg('queries-with.html')}]},
    {id:'views', label:'Views & Materialized Views', cat:'sqladv', imp:2,
     desc:`A view is a stored query that acts like a virtual table, used for abstraction and restricting access to columns; simple views can be updatable. A materialized view stores the result physically for fast reads and must be refreshed, a form of controlled denormalization.`,
     links:[{t:'Docs',u:pg('tutorial-views.html')},{t:'Docs',u:pg('rules-materializedviews.html')}]},
    {id:'procs-triggers', label:'Stored Procedures, Functions & Triggers', cat:'sqladv', imp:2,
     desc:`Code that runs inside the database. Functions return values and can be used in queries; procedures can manage transactions; triggers fire automatically BEFORE or AFTER INSERT/UPDATE/DELETE, typically for auditing or enforcing complex rules. Powerful but hard to test and version, so use them sparingly.`,
     links:[{t:'Docs',u:pg('plpgsql.html')},{t:'Docs',u:pg('trigger-definition.html')}]},
    {id:'sql-injection', label:'SQL Injection & Prepared Statements', cat:'sqladv', imp:2,
     desc:`Concatenating user input into a query string lets attackers change the query ('OR 1=1 --'). Parameterized (prepared) statements send code and data separately, so input can never become SQL. Prepared statements are also parsed and planned once and reused. A security question often attached to DBMS rounds.`,
     links:[{t:'Guide',u:'https://owasp.org/www-community/attacks/SQL_Injection'},{t:'Guide',u:'https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html'}]},

    // ---------- TRANSACTIONS & ISOLATION ----------
    {id:'transaction', label:'Transactions', cat:'txn', imp:4,
     desc:`A sequence of reads and writes treated as one logical unit: a bank transfer's debit and credit either both happen or neither does. Transactions move through states (active, partially committed, committed, failed, aborted) and end in COMMIT or ROLLBACK. Everything in this cluster and the next two is about making many of them safe at once.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Database_transaction'},{t:'Docs',u:pg('tutorial-transactions.html')}]},
    {id:'acid', label:'ACID Properties', cat:'txn', imp:4,
     desc:`Atomicity (all or nothing, ensured by the recovery system's undo), Consistency (constraints hold before and after), Isolation (concurrent transactions don't see each other's partial work, ensured by concurrency control) and Durability (committed data survives crashes, ensured by the log). Expect to explain each with the component that provides it.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/ACID'},{t:'Video',u:yt('ACID properties DBMS explained')}]},
    {id:'txn-states', label:'Transaction States', cat:'txn', imp:2,
     desc:`The state diagram: Active, then Partially Committed after the last statement, then Committed once changes are durable; or Failed, then Aborted after rollback, followed by a restart or kill. A standard short-answer question.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Database_transaction'}]},
    {id:'schedules', label:'Schedules', cat:'txn', imp:3,
     desc:`The interleaved order in which operations of concurrent transactions execute. Serial schedules are correct but slow; concurrent schedules are fast but must be shown equivalent to some serial one. Serializability and recoverability are the two lenses for judging a schedule.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Schedule_(computer_science)'}]},
    {id:'conflict-serializability', label:'Conflict Serializability', cat:'txn', imp:3,
     desc:`Two operations conflict if they belong to different transactions, touch the same item, and at least one is a write. A schedule is conflict serializable if swapping non-conflicting operations turns it into a serial schedule, tested by building a precedence graph and checking it has no cycle. A guaranteed exam question.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Serializability'},{t:'Video',u:yt('conflict serializability precedence graph')}]},
    {id:'view-serializability', label:'View Serializability', cat:'txn', imp:1,
     desc:`A weaker, broader condition: same initial reads, same reads-from relationships and same final writes as some serial schedule. Every conflict-serializable schedule is view serializable, but not the reverse (blind writes). Testing it is NP-complete, so real systems use conflict serializability.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Schedule_(computer_science)#View_equivalence'}]},
    {id:'recoverability', label:'Recoverable & Cascadeless Schedules', cat:'txn', imp:2,
     desc:`Recoverable: a transaction commits only after every transaction it read from has committed. Cascadeless: transactions read only committed data, so one abort can't force others to roll back. Strict: no reads or overwrites of uncommitted writes at all. Each class is contained in the one before; strict 2PL produces strict schedules.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Schedule_(computer_science)#Recoverable'}]},
    {id:'read-anomalies', label:'Read Phenomena', cat:'txn', imp:3,
     desc:`What weak isolation lets through. Dirty read: seeing uncommitted data. Non-repeatable read: the same row returns different values within one transaction. Phantom read: a re-run range query returns new rows. Also lost updates and write skew. The isolation levels are defined by which of these they prevent.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Isolation_(database_systems)#Read_phenomena'}]},
    {id:'isolation-levels', label:'Isolation Levels', cat:'txn', imp:3,
     desc:`SQL-standard levels trade safety for concurrency: Read Uncommitted, Read Committed (no dirty reads), Repeatable Read (no non-repeatable reads) and Serializable. Defaults differ: PostgreSQL uses Read Committed, MySQL InnoDB uses Repeatable Read. PostgreSQL's Serializable uses serializable snapshot isolation (SSI) instead of heavy locking.`,
     links:[{t:'Docs',u:pg('transaction-iso.html')},{t:'Docs',u:'https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-isolation-levels.html'}]},

    // ---------- CONCURRENCY CONTROL ----------
    {id:'locking', label:'Shared & Exclusive Locks', cat:'cc', imp:3,
     desc:`The basic lock-based mechanism: a shared (S) lock allows concurrent reads, an exclusive (X) lock allows one writer and blocks everyone else. Compatibility matrix: S with S is fine, anything with X conflicts. Real systems also use intention locks and multiple granularities (row, page, table).`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Lock_(database)'},{t:'Docs',u:pg('explicit-locking.html')}]},
    {id:'two-phase-locking', label:'Two-Phase Locking (2PL)', cat:'cc', imp:3,
     desc:`A transaction acquires locks in a growing phase and releases them in a shrinking phase, never acquiring after its first release. This guarantees conflict serializability but not freedom from deadlock or cascading aborts. Strict 2PL holds X locks until commit (cascadeless); rigorous 2PL holds all locks until commit.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Two-phase_locking'},{t:'Video',u:yt('two phase locking strict rigorous 2PL')}]},
    {id:'timestamp-ordering', label:'Timestamp Ordering', cat:'cc', imp:2,
     desc:`Each transaction gets a timestamp, and conflicting operations must execute in timestamp order; a transaction that arrives "too late" is rolled back and restarted. Deadlock-free because nothing waits. Thomas's write rule ignores obsolete writes, allowing some view-serializable schedules.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Timestamp-based_concurrency_control'},{t:'Guide',u:'https://en.wikipedia.org/wiki/Thomas_write_rule'}]},
    {id:'mvcc', label:'MVCC', cat:'cc', imp:3,
     desc:`Multiversion concurrency control keeps several versions of each row, so readers see a consistent snapshot and never block writers (and writers don't block readers). Used by PostgreSQL, MySQL InnoDB and Oracle. The cost is garbage collection of old versions: VACUUM in PostgreSQL, purge in InnoDB.`,
     links:[{t:'Docs',u:pg('mvcc-intro.html')},{t:'Guide',u:'https://en.wikipedia.org/wiki/Multiversion_concurrency_control'}]},
    {id:'occ', label:'Optimistic Concurrency Control', cat:'cc', imp:1,
     desc:`Assume conflicts are rare: run without locks, then validate at commit time and abort if another transaction interfered. Suits low-contention workloads. Applications often use the same idea with a version column ("UPDATE … WHERE version = 7").`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Optimistic_concurrency_control'}]},
    {id:'db-deadlock', label:'Deadlocks in Databases', cat:'cc', imp:2,
     desc:`Lock waits can form cycles. Databases prevent them with timestamp schemes (wait-die: older waits, younger dies; wound-wait: older preempts younger), or detect them with a wait-for graph and abort a victim, which is what PostgreSQL and InnoDB do. The same four conditions as in operating systems apply.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Wait-for_graph'},{t:'Docs',u:pg('explicit-locking.html#LOCKING-DEADLOCKS')}]},

    // ---------- RECOVERY ----------
    {id:'recovery', label:'Crash Recovery', cat:'recovery', imp:3,
     desc:`Restoring a consistent state after transaction failures, system crashes or disk loss. Committed transactions must be redone and uncommitted ones undone, which is how the DBMS provides atomicity and durability. The main design question is what gets written to disk and when (steal/no-steal, force/no-force).`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Transaction_log'}]},
    {id:'wal', label:'Write-Ahead Logging', cat:'recovery', imp:3,
     desc:`Log records describing a change must reach stable storage before the changed data pages do, and a transaction commits once its commit record is flushed. The log enables undo and redo, lets pages be written lazily, and doubles as the stream used for replication and point-in-time recovery.`,
     links:[{t:'Docs',u:pg('wal-intro.html')},{t:'Guide',u:'https://en.wikipedia.org/wiki/Write-ahead_logging'}]},
    {id:'checkpoints', label:'Checkpoints', cat:'recovery', imp:2,
     desc:`Periodically flushing dirty pages and recording a checkpoint in the log, so recovery only replays the log from the last checkpoint instead of from the beginning. The trade-off is I/O spikes during checkpoints against shorter recovery times.`,
     links:[{t:'Docs',u:pg('wal-configuration.html')}]},
    {id:'aries', label:'ARIES', cat:'recovery', imp:1,
     desc:`The standard industrial recovery algorithm (IBM, 1992): analysis, redo ("repeat history"), then undo, using log sequence numbers and compensation log records. It supports steal/no-force buffer policies for performance. Good to name in an interview; details are course-level.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Algorithms_for_Recovery_and_Isolation_Exploiting_Semantics'}]},
    {id:'shadow-paging', label:'Shadow Paging', cat:'recovery', imp:1,
     desc:`The log-free alternative: write updates to new copies of pages and atomically swap a page-table pointer at commit. Simple recovery, but it fragments data and makes concurrency hard, so it lost to WAL in general-purpose databases. Copy-on-write file systems and LMDB use the same idea.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Shadow_paging'}]},

    // ---------- STORAGE, INDEXING & QUERIES ----------
    {id:'file-organization', label:'Pages & File Organization', cat:'indexing', imp:2,
     desc:`Tables are stored as fixed-size pages (blocks) on disk, typically 8 KB in PostgreSQL and 16 KB in InnoDB, moved through a buffer pool. Rows can be kept as an unordered heap, sorted, or clustered by key. Since disk I/O dominates cost, query performance is mostly about how many pages must be read.`,
     links:[{t:'Docs',u:pg('storage-page-layout.html')}]},
    {id:'indexing', label:'Indexing', cat:'indexing', imp:4,
     desc:`An auxiliary structure mapping key values to row locations, turning a full scan into a few page reads. Indexes speed up reads, but they slow writes and use space, and an index on a low-selectivity column may never be used. "Why is this query slow?" and "which columns would you index?" are common questions.`,
     links:[{t:'Guide',u:'https://use-the-index-luke.com/'},{t:'Docs',u:pg('indexes.html')}]},
    {id:'index-types', label:'Clustered vs Non-Clustered Indexes', cat:'indexing', imp:3,
     desc:`A clustered (primary) index determines the physical order of rows, so there is only one per table; InnoDB tables are clustered on the primary key. Secondary (non-clustered) indexes store keys plus pointers or primary keys. Dense indexes have an entry per record; sparse indexes have one per block, which only works on sorted data.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Database_index#Clustered'},{t:'Docs',u:'https://dev.mysql.com/doc/refman/8.4/en/innodb-index-types.html'}]},
    {id:'b-tree', label:'B-Tree', cat:'indexing', imp:2,
     desc:`A balanced multiway search tree whose nodes are sized to a disk page, so height stays around 3 or 4 even for millions of keys. Keys and data pointers can live in internal nodes. Questions ask for the order or height given a block size, or to trace insertions with node splits.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/B-tree'},{t:'Tool',u:'https://www.cs.usfca.edu/~galles/visualization/BTree.html'}]},
    {id:'bplus-tree', label:'B+ Tree', cat:'indexing', imp:3,
     desc:`The index structure nearly every relational database uses. All data pointers live in the leaves, internal nodes hold only routing keys (so fanout is higher), and the leaves are linked, so a range scan is one descent plus a sequential walk. "B-tree vs B+ tree" is a top DBMS interview question.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/B%2B_tree'},{t:'Tool',u:'https://www.cs.usfca.edu/~galles/visualization/BPlusTree.html'},{t:'Guide',u:'https://use-the-index-luke.com/sql/anatomy'}]},
    {id:'hash-index', label:'Hash Indexes', cat:'indexing', imp:2,
     desc:`Hash the key to a bucket for O(1) equality lookups, but with no range queries or ordering. Static hashing suffers overflow chains; extendible and linear hashing grow gracefully. Used for in-memory joins and some engine-specific indexes.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Extendible_hashing'},{t:'Docs',u:pg('indexes-types.html')}]},
    {id:'composite-index', label:'Composite & Covering Indexes', cat:'indexing', imp:2,
     desc:`An index on (a, b) is sorted by a, then b, so it serves filters on a or on a and b, but not on b alone (the leftmost-prefix rule). A covering index includes every column a query needs, allowing an index-only scan with no table access. Column order is the key design decision.`,
     links:[{t:'Docs',u:pg('indexes-multicolumn.html')},{t:'Guide',u:'https://use-the-index-luke.com/sql/where-clause/the-equals-operator/concatenated-keys'}]},
    {id:'query-optimization', label:'Query Processing & Optimization', cat:'indexing', imp:3,
     desc:`A query is parsed, rewritten, then planned: the cost-based optimizer uses table statistics to choose access paths (sequential scan, index scan), join order and join algorithms, then the executor runs the plan. EXPLAIN (ANALYZE) shows the chosen plan and is the first tool for diagnosing a slow query.`,
     links:[{t:'Docs',u:pg('using-explain.html')},{t:'Guide',u:'https://en.wikipedia.org/wiki/Query_optimization'}]},
    {id:'join-algorithms', label:'Join Algorithms', cat:'indexing', imp:1,
     desc:`How joins are actually computed: nested loop (good with an index on the inner table or small inputs), sort-merge (inputs already sorted, range conditions) and hash join (large unsorted equi-joins). Block nested-loop cost formulas are a standard exam calculation.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Hash_join'},{t:'Guide',u:'https://en.wikipedia.org/wiki/Sort-merge_join'}]},

    // ---------- NoSQL & SCALING ----------
    {id:'nosql', label:'NoSQL Databases', cat:'dist', imp:3,
     desc:`Non-relational stores built for scale-out or flexible schemas: key-value (Redis), document (MongoDB), wide-column (Cassandra) and graph (Neo4j). They trade joins and sometimes strong consistency for horizontal scaling and schema flexibility. "SQL vs NoSQL, and when to pick which" is a very common question.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/NoSQL'},{t:'Video',u:yt('SQL vs NoSQL when to use')}]},
    {id:'cap', label:'CAP Theorem & BASE', cat:'dist', imp:3,
     desc:`During a network partition, a distributed store must choose between consistency (every read sees the latest write) and availability (every request gets an answer). PACELC adds that even without partitions there's a latency-consistency trade-off. BASE (basically available, soft state, eventual consistency) is the NoSQL counterpoint to ACID.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/CAP_theorem'},{t:'Guide',u:'https://en.wikipedia.org/wiki/Eventual_consistency'}]},
    {id:'replication', label:'Replication', cat:'dist', imp:2,
     desc:`Keeping copies of data on several nodes for availability and read scaling. Leader-follower (primary-replica) is the common setup; synchronous replication is safer, asynchronous is faster but can lose recent writes on failover. Read-your-writes and replication lag are standard follow-up questions.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Replication_(computing)#Database_replication'},{t:'Docs',u:pg('high-availability.html')}]},
    {id:'sharding', label:'Partitioning & Sharding', cat:'dist', imp:3,
     desc:`Splitting data by key: partitioning within one server (range, list, hash), and sharding across servers for write scaling. The hard parts are choosing a shard key that avoids hotspots, cross-shard joins and transactions, and rebalancing (consistent hashing helps).`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Shard_(database_architecture)'},{t:'Docs',u:pg('ddl-partitioning.html')}]},
    {id:'two-phase-commit', label:'Two-Phase Commit (2PC)', cat:'dist', imp:1,
     desc:`Atomic commit across several nodes: a coordinator asks every participant to prepare (vote), then tells all to commit only if all voted yes. It blocks if the coordinator fails after prepare, which is why large systems prefer sagas or consensus (Paxos, Raft). Not to be confused with two-phase locking.`,
     links:[{t:'Guide',u:'https://en.wikipedia.org/wiki/Two-phase_commit_protocol'}]},

    // ---------- DATABASE ENGINES ----------
    {id:'postgresql', label:'PostgreSQL', cat:'engines', imp:3,
     desc:`The most standards-compliant open-source relational database: MVCC, rich SQL (window functions, CTEs, JSONB), extensible types and indexes (GiST, GIN, BRIN), and SSI-based serializable isolation. Its documentation is also one of the best free references for SQL and database internals.`,
     links:[{t:'Docs',u:pg('')},{t:'Repo',u:'https://github.com/postgres/postgres'}]},
    {id:'mysql', label:'MySQL (InnoDB)', cat:'engines', imp:3,
     desc:`The most widely deployed open-source relational database, found in many Indian product companies' stacks. InnoDB, its default engine, provides ACID transactions, row locking, MVCC and a clustered primary-key index. Dialect differences from PostgreSQL (LIMIT syntax, GROUP BY leniency in old versions, INTERSECT added late) sometimes trip up OA answers.`,
     links:[{t:'Docs',u:'https://dev.mysql.com/doc/refman/8.4/en/innodb-storage-engine.html'}]},
    {id:'sqlite', label:'SQLite', cat:'engines', imp:2,
     desc:`A serverless, single-file, embedded SQL database: a library linked into the application, not a separate server. It's in every phone and browser, and the easiest way to practise SQL locally with zero setup. It allows one writer at a time, which is fine for local and embedded use.`,
     links:[{t:'Docs',u:'https://www.sqlite.org/docs.html'},{t:'Guide',u:'https://www.sqlite.org/whentouse.html'}]},
    {id:'mongodb', label:'MongoDB', cat:'engines', imp:2,
     desc:`The best-known document database: JSON-like BSON documents in collections, a flexible schema, embedding instead of joins, replica sets for availability and sharding for scale. It has supported multi-document ACID transactions since 4.0. The usual NoSQL example in interviews.`,
     links:[{t:'Docs',u:'https://www.mongodb.com/docs/manual/'}]},
    {id:'redis', label:'Redis', cat:'engines', imp:2,
     desc:`An in-memory key-value store with rich data types (strings, hashes, lists, sets, sorted sets, streams), mostly used as a cache, session store, rate limiter or leaderboard in front of a relational database. Persistence is optional (RDB snapshots, AOF log). Comes up whenever caching does.`,
     links:[{t:'Docs',u:'https://redis.io/docs/latest/'}]},
    {id:'cassandra', label:'Apache Cassandra', cat:'engines', imp:1,
     desc:`A wide-column store descended from Amazon's Dynamo and Google's Bigtable: leaderless, write-optimised (LSM trees), with per-query tunable consistency (ONE, QUORUM, ALL). The textbook "AP" system in CAP discussions.`,
     links:[{t:'Docs',u:'https://cassandra.apache.org/doc/latest/'}]},

    // ---------- BOOKS, COURSES & PRACTICE ----------
    {id:'korth', label:'Database System Concepts (Korth)', cat:'resources', imp:3,
     desc:`Silberschatz, Korth and Sudarshan, the standard DBMS textbook in Indian universities (Sudarshan is at IIT Bombay). Its chapters map almost one-to-one onto this map: ER, relational model, normalization, SQL, indexing, transactions, concurrency and recovery. The book site hosts slides for quick revision.`,
     links:[{t:'Paper',u:'https://www.db-book.com/'}]},
    {id:'cmu-15445', label:'CMU 15-445 Database Systems', cat:'resources', imp:2,
     desc:`Andy Pavlo's course on how databases work internally (buffer pools, B+ trees, hash tables, query execution, concurrency control, logging and recovery), with free lecture videos. The best resource for depth beyond interview level.`,
     links:[{t:'Guide',u:'https://15445.courses.cs.cmu.edu/'},{t:'Video',u:yt('CMU 15-445 database systems')}]},
    {id:'use-the-index-luke', label:'Use The Index, Luke', cat:'resources', imp:2,
     desc:`Markus Winand's free book on SQL indexing for developers: how B-tree indexes work, why column order matters, and how WHERE, ORDER BY and joins use indexes, across all major databases. The clearest resource for "why is my query slow".`,
     links:[{t:'Guide',u:'https://use-the-index-luke.com/'}]},
    {id:'leetcode-sql', label:'LeetCode SQL 50', cat:'resources', imp:3,
     desc:`LeetCode's study plan of 50 SQL problems graded from basic selects through joins, aggregation, subqueries and advanced string and window functions, plus the wider Database problem set. The closest match to OA-style SQL questions.`,
     links:[{t:'Guide',u:'https://leetcode.com/studyplan/top-sql-50/'}]},
    {id:'hackerrank-sql', label:'HackerRank SQL', cat:'resources', imp:2,
     desc:`A graded track of SQL challenges (basic select, aggregation, joins, advanced), with a skill certification many students add to resumes. HackerRank also hosts many companies' OAs, so its interface is worth getting used to.`,
     links:[{t:'Tool',u:'https://www.hackerrank.com/domains/sql'}]},
    {id:'sqlzoo', label:'SQLZoo & SQLBolt', cat:'resources', imp:2,
     desc:`Free interactive, in-browser SQL tutorials that run each query as you type. SQLBolt is the gentlest starting point; SQLZoo goes further, with join, nested-select and window exercises plus quizzes. Good for your SQL foundations day before moving to LeetCode.`,
     links:[{t:'Tool',u:'https://sqlzoo.net/'},{t:'Tool',u:'https://sqlbolt.com/'}]},
    {id:'pgexercises', label:'PostgreSQL Exercises', cat:'resources', imp:1,
     desc:`Around 80 graded exercises on a single country-club schema, covering joins, aggregates, window functions and recursive queries, run live against a real PostgreSQL instance.`,
     links:[{t:'Tool',u:'https://pgexercises.com/'}]},
    {id:'db-fiddle', label:'DB Fiddle', cat:'resources', imp:1,
     desc:`A browser sandbox where you define a schema and run queries on PostgreSQL, MySQL or SQLite side by side, handy for checking dialect differences or sharing a reproducible query.`,
     links:[{t:'Tool',u:'https://www.db-fiddle.com/'}]}
  ];

  const LINKS = [
    // foundations
    ['dbms','file-vs-dbms'],['dbms','three-schema'],['dbms','data-models'],['dbms','relational-model'],['dbms','sql'],
    ['dbms','transaction'],['dbms','er-model'],['dbms','indexing'],['data-models','relational-model'],['data-models','nosql'],
    ['data-models','er-model'],['three-schema','views'],['three-schema','file-organization'],['file-vs-dbms','acid'],

    // ER
    ['er-model','attributes'],['er-model','cardinality'],['er-model','weak-entity'],['er-model','eer'],['er-model','er-to-tables'],
    ['er-to-tables','relational-model'],['er-to-tables','foreign-key'],['cardinality','er-to-tables'],['weak-entity','keys'],
    ['attributes','1nf'],['attributes','keys'],

    // relational
    ['relational-model','keys'],['relational-model','integrity-constraints'],['relational-model','relational-algebra'],
    ['relational-algebra','relational-calculus'],['relational-algebra','sql'],['relational-algebra','joins'],
    ['relational-algebra','query-optimization'],['keys','foreign-key'],['foreign-key','integrity-constraints'],
    ['keys','closure'],['keys','index-types'],['integrity-constraints','acid'],

    // normalization
    ['fd','closure'],['closure','minimal-cover'],['fd','anomalies'],['anomalies','1nf'],['1nf','2nf'],['2nf','3nf'],
    ['3nf','bcnf'],['bcnf','4nf-5nf'],['fd','decomposition'],['decomposition','bcnf'],['decomposition','3nf'],
    ['minimal-cover','3nf'],['bcnf','denormalization'],['denormalization','views'],['denormalization','nosql'],
    ['relational-model','fd'],['decomposition','joins'],

    // SQL core
    ['sql','sql-sublanguages'],['sql','select-basics'],['sql','group-by'],['sql','joins'],['sql','subqueries'],
    ['sql','set-ops'],['sql','null-handling'],['sql','query-order'],['sql-sublanguages','delete-truncate-drop'],
    ['sql-sublanguages','transaction'],['select-basics','query-order'],['group-by','query-order'],['group-by','null-handling'],
    ['joins','subqueries'],['subqueries','null-handling'],['subqueries','nth-salary'],['set-ops','relational-algebra'],
    ['joins','null-handling'],

    // advanced SQL
    ['sql','window-functions'],['window-functions','nth-salary'],['window-functions','query-order'],['window-functions','group-by'],
    ['sql','cte'],['cte','window-functions'],['sql','views'],['sql','procs-triggers'],['procs-triggers','integrity-constraints'],
    ['sql','sql-injection'],['views','sql-injection'],

    // transactions
    ['transaction','acid'],['transaction','txn-states'],['transaction','schedules'],['schedules','conflict-serializability'],
    ['schedules','view-serializability'],['conflict-serializability','view-serializability'],['schedules','recoverability'],
    ['acid','isolation-levels'],['isolation-levels','read-anomalies'],['acid','recovery'],['acid','wal'],

    // concurrency control
    ['isolation-levels','locking'],['locking','two-phase-locking'],['two-phase-locking','conflict-serializability'],
    ['two-phase-locking','recoverability'],['two-phase-locking','db-deadlock'],['timestamp-ordering','conflict-serializability'],
    ['timestamp-ordering','db-deadlock'],['isolation-levels','mvcc'],['mvcc','timestamp-ordering'],['occ','mvcc'],
    ['occ','schedules'],['locking','db-deadlock'],['read-anomalies','mvcc'],

    // recovery
    ['recovery','wal'],['wal','checkpoints'],['wal','aries'],['recovery','shadow-paging'],['recovery','txn-states'],
    ['wal','replication'],['recoverability','recovery'],

    // indexing
    ['indexing','index-types'],['indexing','bplus-tree'],['indexing','hash-index'],['indexing','composite-index'],
    ['b-tree','bplus-tree'],['file-organization','indexing'],['file-organization','index-types'],['indexing','query-optimization'],
    ['query-optimization','join-algorithms'],['hash-index','join-algorithms'],['bplus-tree','index-types'],
    ['composite-index','bplus-tree'],['query-optimization','joins'],['use-the-index-luke','indexing'],
    ['use-the-index-luke','composite-index'],

    // dist
    ['nosql','cap'],['nosql','sharding'],['nosql','replication'],['cap','replication'],['sharding','two-phase-commit'],
    ['two-phase-commit','acid'],['sharding','hash-index'],['cap','acid'],

    // engines
    ['postgresql','mvcc'],['postgresql','sql'],['postgresql','wal'],['postgresql','isolation-levels'],['mysql','index-types'],
    ['mysql','mvcc'],['mysql','isolation-levels'],['mysql','postgresql'],['sqlite','sql'],['mongodb','nosql'],
    ['mongodb','sharding'],['redis','nosql'],['cassandra','nosql'],['cassandra','cap'],['redis','replication'],

    // resources
    ['korth','dbms'],['korth','bcnf'],['korth','conflict-serializability'],['cmu-15445','bplus-tree'],['cmu-15445','aries'],
    ['cmu-15445','query-optimization'],['leetcode-sql','joins'],['leetcode-sql','window-functions'],['leetcode-sql','nth-salary'],
    ['hackerrank-sql','group-by'],['hackerrank-sql','leetcode-sql'],['sqlzoo','select-basics'],['sqlzoo','joins'],
    ['pgexercises','postgresql'],['pgexercises','cte'],['db-fiddle','postgresql'],['db-fiddle','mysql'],['db-fiddle','sqlite']
  ];

  window.EcosystemMaps = window.EcosystemMaps || {};
  window.EcosystemMaps['dbms-placements'] = {
    slug: 'dbms-placements',
    title: 'DBMS & SQL for Placements Ecosystem Map',
    subtitle: 'ER, keys & normalization · SQL · transactions, concurrency, indexing & NoSQL',
    accent: '#facc15',
    root: 'dbms',
    forces: { linkDistance: 62, linkStrength: 0.42, charge: -300, gravity: 0.028 },
    cats: CATS,
    nodes: NODES,
    links: LINKS
  };
})();
